---
/**
 * Footer (Section 6) — closing chord of the landing page.
 *
 * Figma spec (node 2926:2116):
 *   - Top row: the official FPA logo mark (inline SVG) on the
 *     left, "Kontaktujte nás" CTA on the right.
 *   - Pitch paragraph: Switzer Regular 32px, ~915px wide.
 *     "Ak hľadáte partnera, ktorý spojí mladistvú energiu…"
 *   - 4-column info grid (centered on 1384):
 *       · Kde nás najdete  (Bardejov + Košice addresses)
 *       · Informácie       (s.r.o. + IČO + DIČ)
 *       · Navigácia        (5 site links)
 *       · Sociálne siete   (Facebook + Linkedin)
 *   - Bottom row: All rights reserved · Späť na vrch · Made by NOIRFØRM
 *     (3 evenly spread items at the bottom of the page).
 *
 * Motion is intentionally minimal — footer is functional, not a
 * stage for choreography. Just standard hover states on links and
 * the lenis-powered scroll-to-top behaviour wired in footer.ts,
 * plus a per-word fade-up reveal on the pitch paragraph.
 */

// Pitch broken into per-word spans so each word can fade up
// independently with a small stagger (same word-split technique
// as the About / Leaders manifest scrubs). Layout-safe at any
// resolution: each span is inline-block and wraps as a natural
// word boundary, so line breaks follow the container width
// exactly as they would for plain text.
import { home, langHref, servicesHref, aboutHref, careersHref, type Lang } from "../i18n/home";
import { projectsListHref } from "../i18n/projects";

interface Props {
  lang?: Lang;
}
const { lang = "sk" } = Astro.props;
const t = home[lang];

const pitchLine1 = t.footer.pitch1.split(" ");
const pitchLine2 = t.footer.pitch2.split(" ");

// Google Maps links to the studio's own business listings (exact pin +
// reviews + directions), supplied by the client — more precise than a
// geocoded address search. Locale/tracking params (hl, entry, g_ep)
// stripped so Maps opens in the visitor's own language.
const mapsBardejov =
  "https://www.google.com/maps/place/Fotta+Popadi%C4%8D+Architekt/@49.3041466,21.3043425,17z/data=!3m1!4b1!4m6!3m5!1s0x473e810faeaf144b:0xd85e508bc1807ae5!8m2!3d49.3041466!4d21.3043425!16s%2Fg%2F11ckr41yl0";
const mapsKosice =
  "https://www.google.com/maps/place/Fotta+Popadi%C4%8D+Architekt/@48.7207062,21.2513944,17z/data=!3m1!4b1!4m6!3m5!1s0x473ee1cb2b7bec2f:0xf7123e40b0eda948!8m2!3d48.7207062!4d21.2539693!16s%2Fg%2F11yxspt79w";
---

<footer class="footer" aria-labelledby="footer-heading">
  <h2 class="visually-hidden" id="footer-heading">Pätička</h2>

  {/* IMAGE REVEAL LAYER — full-bleed photo that fades in when the
      a location address block is hovered (see .footer-media CSS,
      driven purely by :has()). One stacked photo per location —
      only the matching one fades in. Photos live in
      public/images/footer/. Decorative → aria-hidden. */}
  <div class="footer-media" aria-hidden="true">
    <img class="footer-media-img" data-image="bardejov" src="/images/footer/bardejov.jpg" alt="" decoding="async" loading="lazy" />
    <img class="footer-media-img" data-image="kosice" src="/images/footer/kosice.jpg" alt="" decoding="async" loading="lazy" />
    <div class="footer-media-overlay"></div>
  </div>

  {/* TOP — brand lockup alone on the left (matches Figma where
      the logo sits at top-7556 with the CTA much lower at 7828). */}
  <a class="footer-logo" href={langHref[lang]} aria-label="Fotta // Popadič — architekt — domov">
    <svg
      class="footer-logo-mark"
      viewBox="37.2 34.9 166.6 125.7"
      fill="currentColor"
      aria-hidden="true"
    >
      <path d="
      <path d="M112.493,87.889c0-3.817-3.107-6.923-6.924-6.923c-3.817,0-6.923,3.106-6.923,6.923v5.119h3.611v-5.119 c0-1.827,1.485-3.312,3.311-3.312s3.313,1.486,3.313,3.312l0.002,0.003l-0.002,8.428h-3.313c-3.817,0-6.923,3.106-6.923,6.923 v5.119c0,3.817,3.105,6.923,6.923,6.923c1.206,0,2.324-0.338,3.313-0.882v0.882h3.611l0.002-27.394L112.493,87.889z M108.882,108.363c0,1.827-1.486,3.312-3.313,3.312s-3.311-1.486-3.311-3.312v-5.119c0-1.827,1.485-3.312,3.311-3.312h3.313 V108.363z"/>
    </svg>
  </a>

  {/* PITCH — large editorial paragraph that opens the footer.
      MASK REVEAL pattern from Hero — each word sits inside an
      overflow:hidden mask so when footer.ts translates the
      word from yPercent:110 to 0 it visibly "rises" from below
      the line. Same parameters as the hero slogan reveal
      (duration 1.0, expo.out, stagger 0.05).
      Layout-safe at any resolution: mask spans are inline-block
      and wrap as natural word boundaries — text flow is
      identical to plain text. */}
  <p class="footer-pitch" data-reveal>
    {pitchLine1.map((w, i) => (
      <>
        <span class="footer-pitch-mask"
          ><span class="footer-pitch-word">{w}</span></span
        >
        {i < pitchLine1.length - 1 ? " " : ""}
      </>
    ))}
    <br />
    {pitchLine2.map((w, i) => (
      <>
        <span class="footer-pitch-mask"
          ><span class="footer-pitch-word">{w}</span></span
        >
        {i < pitchLine2.length - 1 ? " " : ""}
      </>
    ))}
  </p>

  {/* CTA — CENTERED below the pitch (same alignment convention as
      About / Works / Services / Leaders CTAs). Identical button
      spec to all other primary CTAs (211×44, font-weight 500,
      curtain hover + text/arrow shift). */}
  <a class="footer-cta" href="#kontakt" data-open-contact>
    <span class="footer-cta-text">{t.footer.cta}</span>
    <svg
      class="footer-cta-arrow"
      width="16"
      height="10"
      viewBox="0 0 16 10"
      fill="none"
      stroke="currentColor"
      stroke-width="1.5"
      stroke-linecap="round"
      stroke-linejoin="round"
      aria-hidden="true"
    >
      <path d="M0 5 H14 M10 1 L14 5 L10 9" />
    </svg>
  </a>

  {/* 4-COLUMN INFO GRID */}
  <div class="footer-grid">
    {/* Col 1 — Locations */}
    <section class="footer-col">
      <h3 class="footer-col-label">{t.footer.labels.locations}</h3>
      <div class="footer-locations">
        <address class="footer-location" data-footer-image="bardejov">
          <p class="footer-location-name">Bardejov</p>
          <p><a href={mapsBardejov} target="_blank" rel="noopener noreferrer">Priemyselná 3767, 085 01 Bardejov, {t.footer.country}</a></p>
          <p>{t.footer.phone} <a href="tel:+421907183302">+421 907 183 302</a></p>
          <p>{t.footer.email} <a href="mailto:atelier@fottapopadic.sk">atelier@fottapopadic.sk</a></p>
        </address>
        <address class="footer-location" data-footer-image="kosice">
          <p class="footer-location-name">Košice</p>
          <p><a href={mapsKosice} target="_blank" rel="noopener noreferrer">Dominikánske námestie 590/6, 040 01 Staré Mesto, {t.footer.country}</a></p>
        </address>
      </div>
    </section>

    {/* Col 2 — Company info */}
    <section class="footer-col">
      <h3 class="footer-col-label">{t.footer.labels.info}</h3>
      <p class="footer-company">
        Fotta Popadič Architekt s.r.o.<br />
        IČO: 53802012<br />
        DIČ: 2121502680
      </p>
    </section>

    {/* Cols 3+4 — Navigation + Social paired in a tighter sub-grid
        (matches Figma: the two link columns sit closer together as
        one group, separated by the wider 142px gap from the
        address/info columns). */}
    <div class="footer-nav-pair">
      <section class="footer-col">
        <h3 class="footer-col-label">{t.footer.labels.navigation}</h3>
        <ul class="footer-links">
          <li><a href={langHref[lang]} class="nav-flip" data-text={t.footer.nav.home}><span>{t.footer.nav.home}</span></a></li>
          <li><a href={projectsListHref(lang)} class="nav-flip" data-text={t.footer.nav.projects}><span>{t.footer.nav.projects}</span></a></li>
          <li><a href={servicesHref[lang]} class="nav-flip" data-text={t.footer.nav.services}><span>{t.footer.nav.services}</span></a></li>
          <li><a href={aboutHref[lang]} class="nav-flip" data-text={t.footer.nav.about}><span>{t.footer.nav.about}</span></a></li>
          {/* Kariéra — the page doesn't exist yet, so this lands on the
              404 by design (see careersHref in i18n/home.ts for the
              pending Tomáš/Dominik decision and both follow-up paths). */}
          <li><a href={careersHref[lang]} class="nav-flip" data-text={t.footer.nav.careers}><span>{t.footer.nav.careers}</span></a></li>
        </ul>
      </section>

      <section class="footer-col">
        <h3 class="footer-col-label">{t.footer.labels.social}</h3>
        <ul class="footer-links">
          <li>
            <a class="nav-flip" data-text="Facebook" href="https://www.facebook.com/fotta.popadic" target="_blank" rel="noopener noreferrer">
              <span>Facebook</span>
            </a>
          </li>
          <li>
            <a class="nav-flip" data-text="Instagram" href="https://www.instagram.com/fotta_popadic_architekt?igshid=OGQ5ZDc2ODk2ZA%3D%3D" target="_blank" rel="noopener noreferrer">
              <span>Instagram</span>
            </a>
          </li>
        </ul>
      </section>
    </div>
  </div>

  {/* BOTTOM ROW — copyright · back to top · credit.
      "Späť na vrch" uses the same .nav-flip text-flip hover as
      the Navigation column above and the Hero nav links. */}
  <div class="footer-bottom">
    <span class="footer-copyright">All rights reserved</span>
    <button class="footer-totop nav-flip" type="button" data-back-to-top data-text={t.footer.backToTop}>
      <span>{t.footer.backToTop}</span>
    </button>
    <a class="footer-credit" href="https://www.instagram.com/michal.horvath_/" target="_blank" rel="noopener noreferrer">
      Made by NOIRFØRM
    </a>
  </div>
</footer>

<style>
  /* ===== Visually-hidden — accessibility heading not visible
     to sighted users but read by screen readers. */
  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* ===== Section frame =====
     Same outer padding / max-width as other sections so the
     footer aligns to the same canvas. */
  .footer {
    /* relative + isolate so the full-bleed .footer-media layer can
       be absolutely positioned behind the content in a self-
       contained stacking context. overflow:hidden clips the photo
       to the footer box. */
    position: relative;
    isolation: isolate;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    width: 100%;
    /* py-top: 80 at 1440 — matches Works / Services / Leaders for
       consistent rhythm against Figma reference.
       Bottom 28 at 1440 — Figma footer-links sit at bottom-28. */
    padding-block: clamp(48px, 5.55vw, 80px) clamp(20px, 1.94vw, 36px);
    /* Full-bleed box: no 2464 cap on the section itself, so the hover
       photo (.footer-media, inset: 0) reaches the viewport edges on
       screens wider than the canvas. The CONTENT stays on the shared
       2464 canvas regardless — above 2464 the inline gutter grows by
       exactly the strip the old centered max-width used to absorb. */
    padding-inline: max(clamp(16px, 2vw, 48px), calc((100% - 2464px) / 2 + 48px));
    background: var(--bg);
    /* Themeable foreground — defaults to the normal ink colours.
       Every footer text element reads its colour from these two
       custom properties, so the :has() hover rule below only needs
       to flip them to white and the whole footer crossfades in
       unison when the Bardejov block is hovered. */
    --footer-fg: var(--ink);
    --footer-fg-mute: var(--ink-mute);
    color: var(--footer-fg);
  }

  /* ============================================================
     LOGO — top-left, alone (matches Figma where logo is at the
     very top of the footer and the CTA is much further down).
     Left-aligned to the inner section edge (no max-width
     centering) so it lines up with everything else.
     ============================================================ */
  .footer-logo {
    display: flex;
    flex-direction: column;
    /* flex-start, NOT the default stretch — stretch would blow the
       mark up to the full footer width (the svg then centers its
       drawing inside that huge viewport box). */
    align-items: flex-start;
    text-decoration: none;
    color: var(--footer-fg);
    line-height: 1;
    letter-spacing: 0.02em;
    margin: 0;
  }

  /* The official mark, sized to MATCH the hero nav logo (its largest
     state) so the brand reads at the same scale top and bottom of the
     page. currentColor follows --footer-fg, so theming AND the
     location-hover fade both apply. */
  .footer-logo-mark {
    display: block;
    height: clamp(50px, 4.15vw, 88px);
    width: auto;
    aspect-ratio: 166.6 / 125.7;
  }

  /* ===== CTA — CENTERED below the pitch, 48px above (matches
     Services / Leaders CTA spacing exactly). Same 211×44 button
     spec + curtain hover as every other primary CTA. */
  .footer-cta {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: space-between;
    gap: clamp(10px, 0.83vw, 12px);
    width: clamp(180px, 14.65vw, 211px);
    margin-top: clamp(24px, 3.33vw, 48px);
    padding: clamp(10px, 0.97vw, 14px) clamp(12px, 1.11vw, 16px);
    background: var(--btn-bg);
    color: var(--btn-fg);
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: clamp(14px, 1.11vw, 16px);
    line-height: 1;
    letter-spacing: 0.02em;
    overflow: hidden;
    align-self: center;
    will-change: transform;
  }

  .footer-cta::before {
    content: "";
    position: absolute;
    inset: 0;
    background: var(--btn-bg-hover);
    transform: translateX(-101%);
    transition: transform 520ms cubic-bezier(0.65, 0, 0.35, 1);
    will-change: transform;
    z-index: 0;
  }

  .footer-cta:hover::before {
    transform: translateX(0);
  }

  .footer-cta > * {
    position: relative;
    z-index: 1;
  }

  .footer-cta-text {
    display: inline-block;
    transition: transform 420ms cubic-bezier(0.65, 0, 0.35, 1);
    will-change: transform;
  }

  .footer-cta:hover .footer-cta-text {
    transform: translateX(3px);
  }

  .footer-cta-arrow {
    display: block;
    flex: 0 0 auto;
    transition: transform 440ms cubic-bezier(0.65, 0, 0.35, 1);
    will-change: transform;
  }

  .footer-cta:hover .footer-cta-arrow {
    transform: translateX(7px);
  }

  /* ============================================================
     PITCH — large editorial paragraph, exactly 3 lines:
       L1 + L2: sentence 1 (auto-wrapped, balanced via text-wrap)
       L3:      sentence 2 (forced onto its own line via <br/>)
     Font capped at 32px (Figma baseline) so the wrap stays
     consistent at viewports above 1440. Width bumped to 1024
     so sentence 1 fits in 2 balanced lines (~62 chars each)
     instead of leaving "mieste." orphaned on its own line.
     text-wrap: pretty tells the browser to use a slower
     algorithm that specifically avoids short orphan words at
     paragraph end — modern Chromium / WebKit / Firefox honour
     it; older browsers fall back to default wrapping (3 lines
     still happen thanks to the width + font cap).
     ============================================================ */
  .footer-pitch {
    font-family: var(--font-sans);
    font-weight: 400;
    font-style: normal;
    font-size: clamp(22px, 2.22vw, 32px);
    line-height: 1.25;
    letter-spacing: 0.01em;
    color: var(--footer-fg);
    max-width: 1024px;
    width: 100%;
    /* Left-aligned to the inner section edge — no margin-left
       trickery. Lines up with the logo above and the rest of
       the page's left content edge. 40px gap logo → pitch, per
       Figma (node 3290:253) — flat, not fluid (the old 4.44vw
       clamp drifted to ~64px at 1440). */
    margin: 40px 0 0;
    text-wrap: pretty;
  }

  /* ===== Mask-reveal pattern (mirrors Hero slogan) =====
     Each word sits inside .footer-pitch-mask (overflow:hidden)
     and footer.ts translates the inner word from yPercent:110
     to 0 — the word visibly RISES from below the line.

     vertical-align:bottom keeps the masks baseline-aligned with
     surrounding text (default baseline alignment of an inline-
     block with overflow:hidden sits at the bottom of the box,
     which can nudge lines; bottom alignment matches the line's
     bottom edge consistently). */
  .footer-pitch-mask {
    display: inline-block;
    overflow: hidden;
    line-height: inherit;
    vertical-align: bottom;
  }

  /* IMPORTANT: no CSS transform here (same reasoning as
     .hero-title-word — GSAP reads CSS translateY(%) as a pixel
     value via matrix decomposition and stashes it in `y`, so a
     subsequent yPercent tween would leave the pixel offset
     uncleared and the word would never reach yPercent:0).
     opacity:0 alone hides it until footer.ts seeds yPercent
     and runs the reveal. */
  .footer-pitch-word {
    display: inline-block;
    opacity: 0;
    will-change: transform, opacity;
  }

  @media (prefers-reduced-motion: reduce) {
    .footer-pitch-word {
      opacity: 1;
    }
  }

  /* ============================================================
     INFO GRID — 3 column GROUPS, not 4 equal columns.
     Layout proportions taken from Figma:
       · Kde nás najdete : 327/1384 = 23.6%
       · Informácie       : 327/1384 = 23.6%
       · Nav + Social pair: 446/1384 = 32.2% (211 + 25 + 210)
       · Main gap         : 142/1384 = 10.3%
     Inside the pair, Navigation + Sociálne siete share a much
     tighter sub-gap (25px @1440 = 5.6% of the pair's width)
     so they read as ONE column group, not two separate columns
     adrift from the rest.
     ============================================================ */
  .footer-grid {
    display: flex;
    flex-wrap: nowrap;
    width: 100%;
    gap: calc(142 / 1384 * 100%);
    margin: clamp(48px, 5.55vw, 120px) 0 0;
  }

  .footer-col {
    display: flex;
    flex-direction: column;
    gap: clamp(14px, 1.39vw, 24px);
    min-width: 0;
  }

  /* First 2 column groups (Kde nás, Informácie) — each 327/1384
     of the grid width. */
  .footer-grid > .footer-col {
    flex: 0 0 calc(327 / 1384 * 100%);
  }

  /* 3rd group — Nav + Social paired together with their own
     tighter sub-gap. Pair width 446/1384; internal gap 25/446. */
  .footer-nav-pair {
    flex: 0 0 calc(446 / 1384 * 100%);
    display: flex;
    flex-wrap: nowrap;
    gap: calc(25 / 446 * 100%);
    min-width: 0;
  }

  .footer-nav-pair > .footer-col {
    flex: 1 1 0;
    min-width: 0;
  }

  /* Column label — small grey hint above each block */
  .footer-col-label {
    font-family: var(--font-sans);
    font-weight: 400;
    font-style: normal;
    font-size: clamp(13px, 1.11vw, 18px);
    line-height: 1.2;
    letter-spacing: 0.02em;
    color: var(--footer-fg-mute);
    margin: 0;
  }

  /* ===== Locations =====
     Stack of two address blocks (Bardejov + Košice), separated
     by a small gap so they read as distinct locations not one
     long block. */
  .footer-locations {
    display: flex;
    flex-direction: column;
    gap: clamp(20px, 2.22vw, 40px);
  }

  .footer-location {
    font-style: normal;
    font-family: var(--font-sans);
    font-weight: 400;
    /* 20 at 1440 → matches .footer-links and the Figma spec for
       address text (Switzer Regular 20px). */
    font-size: clamp(16px, 1.39vw, 20px);
    line-height: 1.5;
    color: var(--footer-fg);
  }

  .footer-location p {
    margin: 0;
  }

  .footer-location-name {
    /* Same weight as work-title so location name reads as a
       small heading within the block. */
    font-weight: 500;
    font-size: clamp(16px, 1.39vw, 20px);
    line-height: 1.3;
    color: var(--footer-fg);
    margin-bottom: clamp(4px, 0.42vw, 8px) !important;
  }

  .footer-location a {
    color: var(--footer-fg);
    text-decoration: none;
    transition: color 280ms cubic-bezier(0.65, 0, 0.35, 1);
  }

  .footer-location a:hover {
    color: var(--footer-fg-mute);
  }

  /* ===== Company info ===== */
  .footer-company {
    font-family: var(--font-sans);
    font-weight: 400;
    /* Match address + nav-link size — 20 at 1440. */
    font-size: clamp(16px, 1.39vw, 20px);
    line-height: 1.5;
    color: var(--footer-fg);
    margin: 0;
  }

  /* ===== Link lists (nav + social) =====
     gap is 0 — vertical spacing between items is owned entirely
     by the nav-flip's line-height (1.4 = 28px at 20 font). The
     earlier gap of 6px stacked on top of a 30px line-height
     made the list read as too airy compared to the Figma. */
  .footer-links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  /* ===== Generic .nav-flip — base flip behavior reused by every
     hover-flippable element in the footer (nav links, social
     links, and the "Späť na vrch" button). Container is
     overflow:hidden so the duplicate-text from the ::after
     data-text doesn't leak outside the element. Animation
     duration / easing matches Hero's .nav-flip exactly. */
  .nav-flip {
    position: relative;
    display: inline-block;
    overflow: hidden;
    vertical-align: middle;
  }

  .nav-flip span {
    display: inline-block;
    transition: transform 360ms cubic-bezier(0.65, 0, 0.35, 1);
    will-change: transform;
  }

  .nav-flip::after {
    content: attr(data-text);
    position: absolute;
    inset: 0;
    display: inline-block;
    transform: translateY(100%);
    transition: transform 360ms cubic-bezier(0.65, 0, 0.35, 1);
    will-change: transform;
  }

  .nav-flip:hover span,
  .nav-flip:focus-visible span {
    transform: translateY(-100%);
  }

  .nav-flip:hover::after,
  .nav-flip:focus-visible::after {
    transform: translateY(0);
  }

  /* Link-specific font for the nav + social columns.
     line-height 1.4 matches Figma's `leading-normal` on the
     <p> items — tighter than the previous 1.5 so the list
     stacks compactly. */
  .footer-links .nav-flip {
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: clamp(16px, 1.39vw, 20px);
    line-height: 1.4;
    letter-spacing: 0.02em;
    color: var(--footer-fg);
    text-decoration: none;
  }

  /* ============================================================
     BOTTOM ROW — copyright | back to top | credit
     Sits 120px below the info grid (matches Figma) and shares
     the 20px size with the rest of the footer text.
     ============================================================ */
  .footer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: clamp(16px, 2vw, 32px);
    width: 100%;
    /* Spans the full inner section width — copyright at the
       inner-left edge, credit at the inner-right edge,
       Späť na vrch centered. Matches the Figma 3-spread layout.
       120 at 1440 above (8.33vw) — explicit gap to info grid. */
    margin: clamp(56px, 8.33vw, 140px) 0 0;
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: clamp(14px, 1.39vw, 20px);
    line-height: 1.4;
    letter-spacing: 0.02em;
    color: var(--footer-fg);
  }

  .footer-copyright {
    /* Stays left */
  }

  /* Button reset — font + color inherit from .footer-bottom so
     the size automatically matches. The .nav-flip class on this
     button provides the text-flip hover. */
  .footer-totop {
    appearance: none;
    background: none;
    border: 0;
    padding: 0;
    margin: 0;
    cursor: pointer;
    font: inherit;
    color: inherit;
    letter-spacing: inherit;
  }

  .footer-credit {
    text-decoration: none;
    color: var(--footer-fg);
    transition: color 280ms cubic-bezier(0.65, 0, 0.35, 1);
  }

  .footer-credit:hover {
    color: var(--footer-fg-mute);
  }

  /* ============================================================
     LOCATION IMAGE REVEAL — hover the Bardejov address block and a
     full-bleed photo fades in behind the entire footer (the same
     thisisstudiox.com move as the hero image swap). The footer
     EVERYTHING ELSE in the footer fades out — only the hovered
     location block (Bardejov) stays, in white, sitting on the photo
     over a 10% black wash. Pure CSS via :has() — no JS, in keeping
     with the footer's intentionally minimal motion.
     ============================================================ */

  /* Full-coverage media layer — sits behind all footer content
     (content is lifted to z-index:1 below). Hidden until hover. */
  .footer-media {
    position: absolute;
    inset: 0;
    z-index: 0;
    opacity: 0;
    pointer-events: none;
    transition: opacity 600ms cubic-bezier(0.65, 0, 0.35, 1);
  }

  /* Stacked location photos — all start hidden; the hover rules
     below fade in only the one matching the hovered location. The
     parent .footer-media opacity reveals the shared 10% wash. */
  .footer-media-img {
    position: absolute;
    inset: 0;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0;
    transition: opacity 600ms cubic-bezier(0.65, 0, 0.35, 1);
  }

  /* 10% black wash over the photo for text legibility. */
  .footer-media-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.1);
  }

  /* Lift every real child above the media layer. */
  .footer > *:not(.footer-media) {
    position: relative;
    z-index: 1;
  }

  /* Smooth fade for the content that disappears on hover, plus a
     colour crossfade for the surviving location block (its address
     <p>/<a> children inherit / own their own transition). */
  .footer-logo,
  .footer-pitch,
  .footer-cta,
  .footer-bottom,
  .footer-grid > .footer-col,
  .footer-nav-pair,
  .footer-col-label,
  .footer-location,
  .footer-location-name {
    transition:
      opacity 480ms cubic-bezier(0.65, 0, 0.35, 1),
      color 480ms cubic-bezier(0.65, 0, 0.35, 1);
  }

  /* HOVER STATE — a location block (Bardejov or Košice) hovered:
       1. reveal the media layer (its 10% black wash),
       2. fade in that location's matching photo,
       3. flip ONLY the location blocks to white,
       4. fade EVERYTHING else out so only the place text remains.

     The white flip is scoped to .footer-location (not the whole footer)
     on purpose: if the fading elements also turned white they'd gain
     contrast on the darkening photo and linger, so they'd appear to
     leave AFTER the CTA (which never flips). Keeping them on their ink
     colour means they all just drop opacity together. */
  /* The whole reveal exists only on devices with a REAL hover (mouse /
     trackpad) — on touch, a tap merely emulates :hover on the way to
     the Google Maps navigation and the photo would flash (or stick
     after back-navigation). Touch keeps the plain address links
     (Michal, 2026-07-18). */
  @media (hover: hover) {
    .footer:has([data-footer-image]:hover) .footer-location {
      --footer-fg: #fff;
      --footer-fg-mute: rgba(255, 255, 255, 0.72);
    }

    .footer:has([data-footer-image]:hover) .footer-media {
      opacity: 1;
    }

    /* Pick the photo that matches the hovered location. */
    .footer:has([data-footer-image="bardejov"]:hover) .footer-media-img[data-image="bardejov"],
    .footer:has([data-footer-image="kosice"]:hover) .footer-media-img[data-image="kosice"] {
      opacity: 1;
    }

    /* Everything else fades out, leaving the locations column. */
    .footer:has([data-footer-image]:hover) .footer-logo,
    .footer:has([data-footer-image]:hover) .footer-pitch,
    .footer:has([data-footer-image]:hover) .footer-cta,
    .footer:has([data-footer-image]:hover) .footer-bottom,
    .footer:has([data-footer-image]:hover) .footer-grid > .footer-col:not(:has([data-footer-image])),
    .footer:has([data-footer-image]:hover) .footer-nav-pair,
    .footer:has([data-footer-image]:hover) .footer-col:has([data-footer-image]) .footer-col-label {
      opacity: 0;
      pointer-events: none;
    }

    /* The location that is NOT being hovered stays on the photo but
       dimmed — a subtle "disabled" variant, while the hovered one reads
       as active (full white). It stays interactive, so moving the cursor
       onto it swaps the active location (and its photo). */
    .footer:has([data-footer-image]:hover) .footer-location[data-footer-image]:not(:hover) {
      opacity: 0.4;
    }
  }

  /* No hover → the media layer can't ever show; display:none also stops
     the two lazy photos from downloading on touch devices at all. */
  @media (hover: none) {
    .footer-media {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .footer-media {
      transition-duration: 1ms;
    }
  }

  /* ============================================================
     Responsive — the footer is functional, so (unlike the visual
     sections) it reflows on tablet rather than staying a cramped
     scaled-down 4-column desktop.

     TABLET (≤1024): 2-column info grid, stacked bottom row.
     MOBILE (≤767): single-column grid, page-edge 16px gutters.
     ============================================================ */
  @media (max-width: 1024px) {
    .footer {
      padding: 56px 20px 24px;
    }
    .footer-pitch {
      margin-top: 32px;
    }
    .footer-cta {
      margin-top: 32px;
    }
    .footer-grid {
      flex-wrap: wrap;
      gap: 40px;
      margin-top: 48px;
    }
    .footer-grid > .footer-col,
    .footer-nav-pair {
      flex: 0 0 calc(50% - 20px);
    }
    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
      gap: 12px;
      margin-top: 48px;
    }
  }

  @media (max-width: 767px) {
    .footer {
      padding: 56px 16px 24px;
    }
    .footer-grid {
      gap: 32px;
    }
    .footer-grid > .footer-col,
    .footer-nav-pair {
      flex: 0 0 100%;
    }
  }
</style>
